<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">

    <group android:id="@+id/filter" android:checkableBehavior="single">
        <item android:id="@+id/all"
            android:title="All Items" />
        <item android:id="@+id/images"
            android:title="Image Items Only" />
        <item android:id="@+id/text"
            android:title="Text Items Only" />

    </group>
    <item android:id="@+id/action_about" android:title="@string/action_about"
        android:orderInCategory="100" app:showAsAction="never" />
</menu>
